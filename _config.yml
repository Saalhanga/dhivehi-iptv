include:
  - "*.m3u8"
```

And also create a file called `_headers` (no extension) with:
```
/*.m3u8
  Content-Type: application/vnd.apple.mpegurl
  Access-Control-Allow-Origin: *
```

---

## Quickest Solution Right Now

Just use the **raw.githubusercontent.com** URL — it always works reliably:
```
https://raw.githubusercontent.com/Saalhanga/dhivehi-iptv/main/dhiplay.m3u8
